// enum - enumeration is a set of Named Constant object like in js
// a collection of closely related values for reusable code
export enum MatchResult {
  HomeWin = "H",
  AwayWin = "A",
  Draw = "D",
}
